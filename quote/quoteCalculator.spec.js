const expect = require('chai').expect;
const quoteCalculator = require('../quote/quoteCalculator');

/*
describe("Serious Game", () => {
    xit('first test', () => {
        const data = {
            country: 'FR',
            departureDate: '2016-12-08',
            returnDate: '2016-12-10',
            travellerAges: [9],
            options: [],
            cover: 'Basic'
        };
        expect(quoteCalculator(data)).to.eq(3.6);
    });
    xit('second test', () => {
        const secondTest = {
            country: 'FR',
            departureDate: '2017-02-06',
            returnDate: '2017-02-17',
            travellerAges: [32, 47],
            options: [],
            cover: 'Extra'
        };
        expect(quoteCalculator(secondTest)).to.eq(52.80);
    });
    xit('without options test', () => {
        const thirdTest = {
            country: 'NL',
            departureDate: '2017-02-26',
            returnDate: '2017-04-27',
            travellerAges: [77],
            options: [],
            cover: 'Basic'
        };
        expect(quoteCalculator(thirdTest)).to.eq(113.39999999999999);
    });
    xit('with options test', () => {
        const fourTest = {
            country: 'FR',
            departureDate: '2017-03-08',
            returnDate: '2017-03-26',
            travellerAges: [17, 12, 63],
            options: ['Skiing', 'Sports'],
            cover: 'Basic'
        };
        expect(quoteCalculator(fourTest)).to.eq(152.68);
    });
    xit('with options test 2', () => {
        const fourTest = {
            country: 'ES',
            departureDate: '2016-12-24',
            returnDate: '2017-03-13',
            travellerAges: [46],
            options: ['Scuba', 'Sports'],
            cover: 'Basic'
        };
        expect(quoteCalculator(fourTest)).to.eq(217.42);
    });
});
*/